---
apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
resources:
  # vendored from v1.6.1; see the header in crd.yaml before bumping the image
  - crd.yaml
  - release.yaml
  - rbac.yaml
