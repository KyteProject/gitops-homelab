---
apiVersion: helm.toolkit.fluxcd.io/v2
kind: HelmRelease
metadata:
  name: docmost
spec:
  chartRef:
    kind: OCIRepository
    name: app-template
    namespace: flux-system
  interval: 1h
  values:
    controllers:
      docmost:
        annotations:
          reloader.stakater.com/auto: "true"
          glance/description: Wiki
          glance/icon: mdi:book-open-page-variant
          glance/id: docmost
          glance/name: Docmost
          glance/url: https://docmost.omux.io
        initContainers:
          init-db:
            image:
              repository: ghcr.io/home-operations/postgres-init
              tag: 18.6@sha256:b6d3af974df781c673d37e49bdddfa14a6f5be28b18d2cb7713f0449bea4057b
            envFrom: &envFrom
              - secretRef:
                  name: docmost-secret
        containers:
          app:
            image:
              repository: docker.io/docmost/docmost
              tag: 0.96.0@sha256:b56947fcfd08aab8fae12a377e1792784786adbf8b96e4281f14ef4fc072685a
            env:
              APP_URL: https://docmost.omux.io
              MAIL_DRIVER: smtp
              MAIL_FROM_ADDRESS: docmost@omux.io
              MAIL_FROM_NAME: Docmost
              PORT: &port 3000
              TZ: Europe/London
            envFrom: *envFrom
            probes:
              liveness:
                enabled: true
              readiness:
                enabled: true
            resources:
              requests:
                cpu: 25m
                memory: 256Mi
              limits:
                memory: 1Gi
            securityContext:
              allowPrivilegeEscalation: false
              capabilities: { drop: ["ALL"] }
    defaultPodOptions:
      securityContext:
        # The image runs as node (1000) and its data directory is owned by
        # 1000:1000. KOPIUR_MOVER_* in ks.yaml has to match.
        runAsNonRoot: true
        runAsUser: 1000
        runAsGroup: 1000
        fsGroup: 1000
        fsGroupChangePolicy: OnRootMismatch
    persistence:
      data:
        existingClaim: docmost
        globalMounts:
          - path: /app/data/storage
    route:
      app:
        hostnames:
          - "{{ .Release.Name }}.omux.io"
        parentRefs:
          - name: envoy-internal
            namespace: networking
        rules:
          - backendRefs:
              - identifier: app
                port: *port
    service:
      app:
        controller: docmost
        ports:
          http:
            port: *port
